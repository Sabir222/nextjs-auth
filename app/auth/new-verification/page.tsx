import Verification from "@/components/Verification";
import { Suspense } from "react";

const Page = () => {
  return (
    <Suspense>
      <Verification />
    </Suspense>
  );
};

export default Page;
